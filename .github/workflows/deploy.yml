name: Deploy to Render

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # ✅ Esto permite ejecución manual

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: |
          curl -X POST "https://api.render.com/deploy/srv-d9190d8j3s2c739c6a30?key=https://api.render.com/deploy/srv-d9193c19rddc73dcq37g?key=DEEE0nS_c64" \
            -H "Accept: application/json"
